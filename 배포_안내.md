# 런레이더 배포 안내

## 파일 구성
- `index.html` — 메인 페이지 (목록·필터·마감임박/시작임박)
- `race.html` / `race.js` — 대회 상세페이지. 목록에서 대회를 누르면 외부 사이트로 바로 나가지 않고 이 페이지로 먼저 옵니다. 여기서 코스 꿀팁/접수가이드(Tier1)를 보여주고, 맨 아래 "공식 사이트에서 신청하기" 버튼을 눌러야 실제 공식 사이트로 이동합니다.
- `style.css` — 스타일
- `app.js` — 필터·마감임박/시작임박 로직
- `data.json` — 대회 데이터 (170건, 국내 Tier2 161 + 메이저 Tier1 9 — 6대 메이저 전부 포함: 보스턴·도쿄·런던·베를린·시카고·뉴욕. 고러닝·마라톤GO 교차 확인 및 공식 사이트 URL 전수 대조로 완전 중복 8건(대구세계마스터즈 2건·JUST RUN10 세종·스마일런페스티벌·인천송도마라톤·포항 이차전지 마라톤·S-OIL 감동의 마라톤·수원교구 생명사랑 마라톤)을 찾아 제거. Tier1 9곳은 접수 경쟁 강도(competitivenessNote)도 포함. Tier2 전건 마라톤GO/고러닝 상세페이지가 아닌 대회 자체 공식 사이트로 링크 교체 완료 - 전마협 대회(mara1080.com)·JTBC(러너블) 같은 정식 접수 대행 파트너는 그대로 유지. 구로구청장배(다음카페)·울산남구육상연맹(Notion)처럼 특이한 형태의 링크도 실제 접속 확인 후 정상 채널로 확인됨)
- `resources.json` — 대회별 코스 꿀팁(국내 26개: Tier1 3개 + Tier2 23개)·접수가이드(해외 6개) 콘텐츠, 상세페이지에서 사용. 국내 Tier2 23개는 실제 후기·기사 검색 기반으로 작성 - 나머지 Tier2 대회는 아직 꿀팁 없음
- `fee_capacity.json` — 대회별 참가비·정원 정보(현재 10개 대회 확보), 확실히 확인된 수치만 반영 - 상세페이지에 💰참가비/👥정원 행으로 노출
- `rebuild_all.py` — 크롤링 원본(gorunning_extracted_sample.csv, marathongo_raw.txt)으로 data.json을 다시 만드는 스크립트

개발 지식 없이도 이 7개 파일(`index.html`, `race.html`, `style.css`, `app.js`, `race.js`, `data.json`, `resources.json`)만 있으면 배포할 수 있습니다.

이번 버전(v20)에서 추가된 것: 로그인 없이 브라우저에 저장되는 "찜하기" 기능(목록·상세페이지 모두), 거리 정보 없는 대회는 "미정" 대신 "거리 미확인"으로 표기, 주최 정보 없는 대회는 "-" 대신 "정보 없음"으로 표기, "최종확인" 날짜에 무엇을 확인했는지 설명 문구 추가.

## 가장 쉬운 배포 방법: Netlify Drop (5분)

1. 브라우저에서 https://app.netlify.com/drop 접속
2. 구글/깃허브 계정으로 간단히 로그인 (또는 이메일 가입)
3. `index.html`, `style.css`, `app.js`, `data.json` 4개 파일이 든 폴더를 통째로 화면에 드래그 앤 드롭
4. 몇 초 뒤 `https://무작위이름.netlify.app` 같은 실제 접속 가능한 주소가 발급됩니다
5. Netlify 설정에서 "Change site name"으로 원하는 이름으로 바꾸거나, 실제 도메인(예: runradar.kr)을 구매해 연결할 수 있습니다 (도메인 구매는 가비아·후이즈 등에서 가능, 연 1~2만원 수준)

## 데이터 갱신 방법 (수동, 현재 단계)

지금은 크롤링이 자동화되어 있지 않습니다. 최신 데이터로 갱신하려면:

1. 고러닝(gorunning.kr), 마라톤GO(marathongo.co.kr) 등에서 새 데이터를 다시 수집
2. `rebuild_all.py`를 다시 실행해 `data.json`을 새로 생성
3. Netlify Drop 화면에 새 폴더를 다시 드래그하면 재배포됩니다 (같은 사이트 주소 유지)

이 과정을 매일 자동으로 돌리려면(예: 새벽에 자동으로 크롤링 → 배포) 서버 자동화가 필요하며, 이 단계부터는 개발자의 도움이 유용해집니다. MVP 단계에서는 수동 갱신으로 충분합니다.

## ⚠️ 지금 사이트가 비공개 상태일 수 있음

Netlify 화면 상단에 "Only project members can view this site and its deploy previews. Go live or manage access in Netlify."라는 배너가 보인다면, 지금은 프로젝트 멤버(로그인한 본인)만 사이트를 볼 수 있고 다른 사람은 접속이 안 되는 상태입니다. Netlify 사이트 설정에서 배너의 "Go live" 또는 "manage access" 링크를 눌러 공개 접근을 켜야 다른 사람도 볼 수 있습니다.

## 알아둘 점

- 지금 사이트는 정적 파일이라 로그인, 찜하기, 알림 같은 개인화 기능은 아직 없습니다 (3단계 계획 항목).
- 브랜드명은 "런레이더"로 확정했습니다.
- 로드런(roadrun.co.kr)은 옛날 인코딩(EUC-KR) 문제로 아직 데이터에 포함되지 않았습니다. (샌드박스 네트워크 정책상 우회 접근도 막혀있어 현재 기술로는 해결 불가 - 원문 그대로 재확인 필요)
- 러닝위키(runningwikii.com)는 페이지에 "무단 복사를 금합니다" 저작권 경고가 명시되어 있어 데이터를 긁어오지 않았습니다. (교차 확인용 참고 링크로만 활용 예정)
- 랭킹마라톤(rankingmarathon.com)은 자바스크립트로 목록을 그리는 사이트라 지금 도구로는 빈 화면만 가져와집니다 - 추후 필요하면 브라우저 기반 수집이 필요합니다.
- 전국마라톤협회(mara1080.com)도 같은 이유(JS 렌더링)로 자동 수집이 안 됩니다.
